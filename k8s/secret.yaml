apiVersion: v1
kind: Secret
metadata:
  name: lei-monitor-secret
  namespace: lei-monitor
type: Opaque
stringData:
  # Placeholder only. In production this secret must be managed via the
  # Azure Key Vault Secrets Store CSI driver and must not be stored in plain YAML.
  CustomerStorage: "<SQL_SERVER_CONNECTION_STRING>"
