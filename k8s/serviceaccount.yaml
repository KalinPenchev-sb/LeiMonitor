apiVersion: v1
kind: ServiceAccount
metadata:
  name: lei-monitor-sa
  namespace: lei-monitor
  annotations:
    # Replace with the client ID of the Azure Managed Identity once Workload Identity is configured
    azure.workload.identity/client-id: "<AZURE_MANAGED_IDENTITY_CLIENT_ID>"
